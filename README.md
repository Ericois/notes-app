### Notes-app

## Setup 
First clone the repo: 
```
git clone <repo-url>
```
Then install the requirements:
```
pip install -r requirements.txt

```
Then run the app:

```
python main.py
```
The app can then be viewed at http://127.0.0.1:5000



